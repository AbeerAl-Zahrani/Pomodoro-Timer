export default function PomodoroTimerLayout({ children }: Readonly<{ children: React.ReactNode }>) {
    return <div style={{display:'flex',justifyContent:'center',alignItems:'center'}}>{children}</div>
}